[pytest]
addopts = --ignore=services
markers =
    basic: basic tests
    llnl_only: mark a test excluded when runs outside the LLNL environment.
    gpu_required: mark test as GPU required.
    moe_required: mark test as MOE required.
    slurm_required: mark test as SLURM required